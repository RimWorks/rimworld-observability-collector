name: dependabot-automerge

on: pull_request_target

# A called workflow cannot hold more than its caller, and the repo default is read.
permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: RimWorks/mod-ci/.github/workflows/dependabot-automerge.yml@eae5a8f32bee86128ae88bf8821ebaca65ddc1e9 # v1.4.1
