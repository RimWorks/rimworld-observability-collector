name: dependabot-automerge

on: pull_request_target

# A called workflow cannot hold more than its caller, and the repo default is read.
permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: RimWorks/mod-ci/.github/workflows/dependabot-automerge.yml@5698991b43ade4de809d49d9695ac88e0895b298 # v1.2.1
