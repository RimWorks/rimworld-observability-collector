<Project>

  <!-- CI already gets these rules via dotnet-sonarscanner; this makes them show up locally too. -->
  <ItemGroup>
    <PackageReference Include="SonarAnalyzer.CSharp" Version="10.33.0.1635" PrivateAssets="all" />
  </ItemGroup>

</Project>
