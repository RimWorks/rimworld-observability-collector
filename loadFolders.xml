<?xml version="1.0" encoding="utf-8"?>
<loadFolders>
    <v1.6>
        <li>/</li>
        <!-- only the backend whose library is present loads, so the other one's missing
             assembly never has to resolve. with both active, PatchBackends picks Concord. -->
        <li IfModActive="brrainz.harmony">Harmony</li>
        <li IfModActive="concordlib.concord">Concord</li>
    </v1.6>
</loadFolders>
